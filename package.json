{
  "name": "workspace",
  "version": "1.0.0",
  "description": "A modern, multi-user expense tracking application that imports bank transactions from PDF statements and provides detailed analytics and spending reports.",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cspell/cspell-bundled-dicts": "^10.0.0",
    "@playwright/test": "^1.61.1",
    "playwright": "^1.61.1"
  }
}
